<?php

declare(strict_types=1);


// . O . .,
// O X X X,
// O O O .,
// X X O X,
// . O X .,
function winner(array $lines): ?string
{
    $width = strlen(trim($lines[0]));

    $rows = count($lines);
    $columns = (($width - 1)/2) + 1 ;

    // for ($i=0; $i < $rows; $i++) { 
    //     var_dump();
    // }
    die;

    return null;
}
